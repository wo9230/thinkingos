# CLAUDE.md

本文件是 **Claude Code** 在本仓库的入口约定。（Codex 读 `AGENTS.md`。）

**事实源只有一份，不在这里复制** —— 项目定位、开工必读清单、五条保命规则全部在 @AGENTS.md，先读它。

改规则去改 `AGENTS.md`，别在本文件里另写一套。

---

## 只对 Claude Code 生效的部分

### Skill

`clarify` 的**主版本在 `.agents/skills/clarify/SKILL.md`**（Codex 的发现路径）。
`.claude/skills/clarify/SKILL.md` 与同目录 `references/` 都是指向它的符号链接——两边同一份内容，改主版本即可。

不要把这两个文件分别编辑，会造成漂移。

### 最容易犯的错（本项目历史教训）

写模板、框架、文档时，**不要把用户给的素材当成内容写进去**。

用户喂逐字稿、聊天记录、他自己的业务数据，是为了让你听懂他要做什么，**不是让你把它变成产品的一部分**。产品文件必须领域中立。

需要举例时编中性例子，或指向 `cases/`。完整规则见 @AGENTS.md「隐私铁律」。

### 输出

中文。文件路径、命令、术语原名保持英文。
